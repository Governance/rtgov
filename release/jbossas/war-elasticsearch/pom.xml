<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  	<modelVersion>4.0.0</modelVersion>
	<groupId>org.overlord.rtgov.release.jbossas</groupId>
	<artifactId>overlord-rtgov-elasticsearch</artifactId>
	<packaging>war</packaging>
	<version>2.0.0-SNAPSHOT</version>
	<name>Overlord RTGov::Release::JBossAS::WAR ElasticSearch</name>

	<parent>
		<groupId>org.overlord.rtgov.release</groupId>
		<artifactId>jbossas</artifactId>
		<version>2.0.0-SNAPSHOT</version>
	</parent>

	<dependencies>		
		<dependency>
			<groupId>org.overlord.rtgov.content.services</groupId>
			<artifactId>elasticsearch-rests</artifactId>
			<version>${project.version}</version>
		</dependency>
		
		<!-- Overlord commons -->
		
		<dependency>
			<groupId>org.overlord</groupId>
			<artifactId>overlord-commons-auth</artifactId>
			<exclusions>
				<exclusion>
					<groupId>commons-codec</groupId>
					<artifactId>commons-codec</artifactId>
				</exclusion>
			</exclusions>
		</dependency>
		<dependency>
			<groupId>org.overlord</groupId>
			<artifactId>overlord-commons-auth-jboss7</artifactId>
			<exclusions>
				<exclusion>
					<groupId>commons-codec</groupId>
					<artifactId>commons-codec</artifactId>
				</exclusion>
			</exclusions>
		</dependency>
		<dependency>
			<groupId>org.overlord</groupId>
			<artifactId>overlord-commons-auth-tomcat7</artifactId>
			<exclusions>
				<exclusion>
					<groupId>commons-codec</groupId>
					<artifactId>commons-codec</artifactId>
				</exclusion>
			</exclusions>
		</dependency>
		<dependency>
			<groupId>org.overlord</groupId>
			<artifactId>overlord-commons-config</artifactId>
			<exclusions>
				<exclusion>
					<groupId>commons-io</groupId>
					<artifactId>commons-io</artifactId>
				</exclusion>
				<exclusion>
					<groupId>commons-configuration</groupId>
					<artifactId>commons-configuration</artifactId>
				</exclusion>
			</exclusions>
		</dependency>

	        <!-- Test dependencies -->
		
		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<finalName>${project.artifactId}</finalName>

		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-war-plugin</artifactId>
				<configuration>
 					<archive>
						<manifestEntries>
							<!-- NOTE: Dependency on org.apache.commons.codec added
							as a runtime dependency for overlord-commons-auth. If
							this is changed to a module dependency, then the
							direct dependency on codec can be removed -->
							<Dependencies>org.osgi.core, org.apache.commons.configuration, deployment.overlord-rtgov.war</Dependencies>
						</manifestEntries>
					</archive>
				</configuration>
			</plugin>
		</plugins>

	</build>

</project>
