// Key is null when the event source is a node within an EPN,
// so need to derive the key from the event (value)

String customer=value.substring(1);

boolean add=(value.charAt(0) == '+');
	
if (add) {
	acs.insert(customer, customer);
} else {
	acs.remove(customer, customer);
}
